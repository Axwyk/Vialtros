name: Deploy Pages

on:
  push:
    branches: [master]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install
        run: npm ci
        working-directory: frontend

      - name: Verify Google Maps env vars
        run: |
          [ -n "$REACT_APP_GOOGLE_MAPS_API_KEY" ] && echo "REACT_APP_GOOGLE_MAPS_API_KEY: SET" || echo "REACT_APP_GOOGLE_MAPS_API_KEY: MISSING"
          [ -n "$REACT_APP_GOOGLE_MAPS_MAP_ID" ]  && echo "REACT_APP_GOOGLE_MAPS_MAP_ID: SET"  || echo "REACT_APP_GOOGLE_MAPS_MAP_ID: MISSING"
        env:
          REACT_APP_GOOGLE_MAPS_API_KEY: ${{ secrets.REACT_APP_GOOGLE_MAPS_API_KEY }}
          REACT_APP_GOOGLE_MAPS_MAP_ID: ${{ secrets.REACT_APP_GOOGLE_MAPS_MAP_ID }}

      - name: Build
        run: npm run build
        working-directory: frontend
        env:
          CI: false
          REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}
          REACT_APP_WS_URL: ${{ secrets.REACT_APP_WS_URL }}
          REACT_APP_GOOGLE_MAPS_API_KEY: ${{ secrets.REACT_APP_GOOGLE_MAPS_API_KEY }}
          REACT_APP_GOOGLE_MAPS_MAP_ID: ${{ secrets.REACT_APP_GOOGLE_MAPS_MAP_ID }}

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/build

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4